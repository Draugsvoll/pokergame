#rendering
villain action text -> set at villainAct() function (game-tree)
villain bet text -> in renderBets()
render hero actions at ACTION-BUTTONS
handStrengths updated at end of DealCards() functions
render button bar at every dealHand()
BUTTON BAR hiding at every BUTTON click AND 



#logic
hero call blind -> villain coded to call pre
villainAct() -> whenever we want villain to REACT
currentStreet incremented inside dealCards() functions


# TODO
villain doesnt act first by default -> check for isVillainDealer on every dealCards() function and run villainAct
villain dont react properly to 3-bet


